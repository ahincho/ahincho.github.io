{
  "extends": "astro/tsconfigs/strict",
  "include": [".astro/types.d.ts", "**/*"],
  "// worker, chat": "Deployed on their own and typed against the Workers runtime, so each carries its own tsconfig.",
  "exclude": ["dist", "worker", "chat"]
}
