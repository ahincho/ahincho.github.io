# Base URL of the Cloudflare Worker that stores the public counters.
# Leave it empty to build the site without any counter (the hero terminal
# simply omits the visits line).
# Local development: run `pnpm dev` inside worker/ and use http://127.0.0.1:8787
PUBLIC_COUNTER_URL=
