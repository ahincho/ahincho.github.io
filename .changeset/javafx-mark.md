---
'ahincho-portfolio': patch
---

Show the Java mark for JavaFX in the ByteXByte experience, where the tag was rendering with no icon at all. Neither icon set ships a JavaFX logo, and it is Java's UI toolkit, so it borrows the Java mark the same way Amazon Bedrock borrows the AWS one.
