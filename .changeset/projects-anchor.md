---
'ahincho-portfolio': patch
---

Rename the landing anchor from `#project` to `#projects` and put the navigation label in the plural, now that the section holds more than one. The footer also stays at the bottom of short pages instead of floating mid-screen.
