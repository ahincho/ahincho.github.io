---
'ahincho-portfolio': minor
---

Add a social sharing card. Links to the portfolio previewed as a blank box on LinkedIn, WhatsApp and Slack; they now carry an image, the page's own URL and a site name. The card is generated by `pnpm og` and committed, so a deploy never depends on which fonts the runner has installed.
