---
'ahincho-portfolio': minor
---

Move the Spark Match case study onto its own page at `/projects/spark-match/`, leaving the landing with a summary, the video and a link through. Screenshots now render at full column width instead of quarter width, where their text was too small to read. Canonical and hreflang tags are derived from the current path, so the new pages point at the right alternates, and the language switcher keeps visitors on the page they were reading.
